node_modules/
data/
.env
*.mcpb

# dist/index.js and dist/sql-wasm.wasm stay committed (small, and let a
# git clone run `node dist/index.js` without a bundler). The Go bridge
# binaries are large, platform-specific build artifacts — never commit them.
# The release workflow cross-compiles them fresh for every target; locally
# `npm run build` produces the one for your host.
dist/hermeneia-bridge
dist/hermeneia-bridge-*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
